cluster:
  indexNodes:
    - id: "0"
      host: "localhost"
      port: 5000
    - id: "1"
      host: "localhost"
      port: 5001

queryNode:
  client:
    host: "localhost"
    ports: [ 6000, 6001 ]

indexNode:
  client:
    host: "localhost"
    ports: [ 5000, 5001 ]

ml:
  models:
    textEmbedding:
      url: "djl://ai.djl.huggingface.pytorch/sentence-transformers/all-MiniLM-L6-v2"
      engine: "PyTorch"
